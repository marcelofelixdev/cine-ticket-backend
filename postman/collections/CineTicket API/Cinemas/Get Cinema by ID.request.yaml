$kind: http-request
name: Get Cinema by ID
method: GET
url: '{{baseUrl}}/api/v1/cinemas/{{cinemaId}}'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const status = pm.response.code;
      const json = pm.response.json();

      if (status === 200) {
          pm.test("Status é 200 (OK)", function () {
              pm.expect(status).to.equal(200);
          });

          pm.test("Body contém id", function () {
              pm.expect(json).to.have.property("id");
          });

          pm.test("Body contém nome", function () {
              pm.expect(json).to.have.property("nome");
          });

          pm.test("Body contém cnpj", function () {
              pm.expect(json).to.have.property("cnpj");
          });

          pm.test("Body contém endereco", function () {
              pm.expect(json).to.have.property("endereco");
          });

          pm.test("Content-Type é application/json", function () {
              pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");
          });

          pm.test("Tempo de resposta menor que 2000ms", function () {
              pm.expect(pm.response.responseTime).to.be.below(2000);
          });

      } else if (status === 401) {
          pm.test("Status é 401 (Não autorizado)", function () {
              pm.expect(status).to.equal(401);
          });

      } else if (status === 500) {
          pm.test("Status é 500 (Erro interno — ex: Cinema não encontrado)", function () {
              pm.expect(status).to.equal(500);
          });

          pm.test("Body contém campo message com 'Cinema não encontrado'", function () {
              pm.expect(json).to.have.property("message");
              pm.expect(json.message).to.include("Cinema não encontrado");
          });

      } else {
          pm.test("Status inesperado: " + status, function () {
              pm.expect.fail("Status não mapeado nos testes: " + status);
          });
      }
